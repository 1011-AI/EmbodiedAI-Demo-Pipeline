diff --git a/src/openpi/models/siglip.py b/src/openpi/models/siglip.py
index c74c99e..bacb4e6 100644
--- a/src/openpi/models/siglip.py
+++ b/src/openpi/models/siglip.py
@@ -124,12 +124,15 @@ class Encoder(nn.Module):
         out = {}

         if self.scan:
-            block = nn.remat(
-                Encoder1DBlock,
-                prevent_cse=False,
-                static_argnums=(2,),  # 0=self, 2=deterministic
-                policy=getattr(jax.checkpoint_policies, self.remat_policy, None),
-            )
+            if self.remat_policy == "none":
+                block = Encoder1DBlock
+            else:
+                block = nn.remat(
+                    Encoder1DBlock,
+                    prevent_cse=False,
+                    static_argnums=(2,),  # 0=self, 2=deterministic
+                    policy=getattr(jax.checkpoint_policies, self.remat_policy),
+                )
             x, scan_out = nn.scan(
                 block,
                 variable_axes={"params": 0},
diff --git a/src/openpi/policies/b1k_policy.py b/src/openpi/policies/b1k_policy.py
index 901fa31..f5d9d0c 100644
--- a/src/openpi/policies/b1k_policy.py
+++ b/src/openpi/policies/b1k_policy.py
@@ -2,11 +2,26 @@ import dataclasses

 import einops
 import numpy as np
-from omnigibson.learning.utils.eval_utils import PROPRIOCEPTION_INDICES
-
 from openpi import transforms
 from openpi.models import model as _model

+
+# Keep the checkpoint's original BEHAVIOR R1Pro proprio contract without
+# importing the full OmniGibson simulator in a data-only training image.
+# The action order is intentionally different: left_gripper remains action
+# dimension 14, while the checkpoint tokenizes state grippers at dimensions
+# 21 and 22 (after both seven-DoF arms).
+PROPRIOCEPTION_INDICES = {
+    "R1Pro": {
+        "base_qvel": list(range(0, 3)),
+        "trunk_qpos": list(range(53, 57)),
+        "arm_left_qpos": list(range(3, 10)),
+        "arm_right_qpos": list(range(28, 35)),
+        "gripper_left_qpos": list(range(24, 26)),
+        "gripper_right_qpos": list(range(49, 51)),
+    }
+}
+
 MAX_DEPTH = 10.0

 CAMERA_INTRINSICS = {
diff --git a/src/openpi/models/gemma.py b/src/openpi/models/gemma.py
--- a/src/openpi/models/gemma.py
+++ b/src/openpi/models/gemma.py
@@ -169,6 +169,7 @@ class Attention(nn.Module):
     """Attention module."""

     configs: Sequence[Config]
+    implementation: str = "einsum"

     @nn.compact
     def __call__(self, xs, positions, attn_mask, kv_cache):
@@ -223,22 +224,41 @@ class Attention(nn.Module):
             k = jnp.concatenate([cache_k, k], axis=1)
             v = jnp.concatenate([cache_v, v], axis=1)

-        q = einops.rearrange(q, "B T (K G) H -> B T K G H", K=self.configs[0].num_kv_heads)
-        logits = jnp.einsum("BTKGH,BSKH->BKGTS", q, k, preferred_element_type=jnp.float32)
-
         if attn_mask.shape != (q.shape[0], 1, q.shape[1], k.shape[1]):
             raise ValueError(
                 f"Attention mask with shape {attn_mask.shape} but shapes for q and k are: {q.shape} and {k.shape}"
             )
-
-        # big_neg = jnp.finfo(logits.dtype).min
-        big_neg = -2.3819763e38  # See gemma/modules.py
-        masked_logits = jnp.where(attn_mask[:, :, None, :, :], logits, big_neg)
-
-        probs = jax.nn.softmax(masked_logits, axis=-1).astype(dtype)
-
-        encoded = jnp.einsum("BKGTS,BSKH->BTKGH", probs, v)
-        encoded = einops.rearrange(encoded, "B T K G H -> B T (K G) H")
+        if self.implementation == "einsum":
+            grouped_q = einops.rearrange(
+                q,
+                "B T (K G) H -> B T K G H",
+                K=self.configs[0].num_kv_heads,
+            )
+            logits = jnp.einsum(
+                "BTKGH,BSKH->BKGTS",
+                grouped_q,
+                k,
+                preferred_element_type=jnp.float32,
+            )
+            # big_neg = jnp.finfo(logits.dtype).min
+            big_neg = -2.3819763e38  # See gemma/modules.py
+            masked_logits = jnp.where(attn_mask[:, :, None, :, :], logits, big_neg)
+            probs = jax.nn.softmax(masked_logits, axis=-1).astype(dtype)
+            encoded = jnp.einsum("BKGTS,BSKH->BTKGH", probs, v)
+            encoded = einops.rearrange(encoded, "B T K G H -> B T (K G) H")
+        elif self.implementation in {"xla", "cudnn"}:
+            # q already contains the 1/sqrt(head_dim) scale above, so pass 1.0
+            # to preserve the released implementation's exact convention.
+            encoded = jax.nn.dot_product_attention(
+                q,
+                k,
+                v,
+                mask=attn_mask,
+                scale=1.0,
+                implementation=self.implementation,
+            )
+        else:
+            raise ValueError(f"Unknown attention implementation: {self.implementation}")

         out = []
         start = 0
@@ -298,13 +318,18 @@ class Block(nn.Module):

     dropout: float = 0.0
     dropout_bdims: tuple[int, ...] = ()
+    attention_implementation: str = "einsum"

     @nn.compact
     def __call__(self, xs, kv_cache, positions, attn_mask, adarms_cond, deterministic=True):  # noqa: FBT002
         xs = sharding.activation_sharding_constraint(xs)
         drop = nn.Dropout(self.dropout, self.dropout_bdims) if self.dropout else lambda x, _: x

-        attn = Attention(configs=self.configs, name="attn")
+        attn = Attention(
+            configs=self.configs,
+            implementation=self.attention_implementation,
+            name="attn",
+        )

         pre_attn = []
         gates = []
@@ -356,6 +381,8 @@ class Module(nn.Module):
     dropout: float = 0.0
     dropout_bdims: tuple[int, ...] = ()  # Every float is dropped independently.
     adarms: bool = False
+    remat_policy: str = "nothing_saveable"
+    attention_implementation: str = "einsum"

     def setup(self):
         # all experts must have the same depth
@@ -366,12 +393,15 @@ class Module(nn.Module):
             embed_dim=self.configs[0].width,  # embedder for first expert only
             name="embedder",
         )
-        block_cls = nn.remat(
-            Block,
-            prevent_cse=False,
-            static_argnums=(5,),  # 0=self, 6=deterministic
-            policy=jax.checkpoint_policies.nothing_saveable,
-        )
+        if self.remat_policy == "none":
+            block_cls = Block
+        else:
+            block_cls = nn.remat(
+                Block,
+                prevent_cse=False,
+                static_argnums=(5,),  # 0=self, 6=deterministic
+                policy=getattr(jax.checkpoint_policies, self.remat_policy),
+            )
         self.layers = nn.scan(
             block_cls,
             variable_axes={"params": 0},
@@ -388,6 +418,7 @@ class Module(nn.Module):
             configs=self.configs,
             dropout=self.dropout,
             dropout_bdims=self.dropout_bdims,
+            attention_implementation=self.attention_implementation,
         )
         self.final_norms = [RMSNorm(name=_name("final_norm", i)) for i in range(len(self.configs))]

diff --git a/src/openpi/models/pi0.py b/src/openpi/models/pi0.py
--- a/src/openpi/models/pi0.py
+++ b/src/openpi/models/pi0.py
@@ -77,6 +77,8 @@ class Pi0(_model.BaseModel):
                 configs=[paligemma_config, action_expert_config],
                 embed_dtype=config.dtype,
                 adarms=config.pi05,
+                remat_policy=config.gemma_remat_policy,
+                attention_implementation=config.attention_implementation,
             )
         )
         llm.lazy_init(rngs=rngs, method="init", use_adarms=[False, True] if config.pi05 else [False, False])
@@ -86,6 +88,7 @@ class Pi0(_model.BaseModel):
                 variant="So400m/14",
                 pool_type="none",
                 scan=True,
+                remat_policy=config.siglip_remat_policy,
                 dtype_mm=config.dtype,
             )
         )
diff --git a/src/openpi/models/pi0_config.py b/src/openpi/models/pi0_config.py
--- a/src/openpi/models/pi0_config.py
+++ b/src/openpi/models/pi0_config.py
@@ -37,6 +37,12 @@ class Pi0Config(_model.BaseModelConfig):
     # whether to use pointnet to encode the point cloud
     pcd: bool = False

+    # Runtime-only graph policies. They do not change checkpoint parameter
+    # names or shapes, but are part of the Demo Pipeline resume contract.
+    gemma_remat_policy: str = "nothing_saveable"
+    siglip_remat_policy: str = "nothing_saveable"
+    attention_implementation: str = "einsum"
+
     def __post_init__(self):
         if self.max_token_len is None:
             object.__setattr__(self, "max_token_len", 200 if self.pi05 else 48)
